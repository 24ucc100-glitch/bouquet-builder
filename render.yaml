services:
  - type: web
    name: bouquet-builder-backend
    env: node
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
